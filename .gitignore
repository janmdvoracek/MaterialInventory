__pycache__/
*.pyc
.claude
.venv/
.vscode*
venv/
db.sqlite3
# STATIC_ROOT — collectstatic output, generated. Not `static/`, which is the
# hand-maintained source dir in STATICFILES_DIRS and must stay tracked.
staticfiles/
# Working spreadsheet parked under static/; not a served asset. Excluded from
# the image too (.dockerignore) so collectstatic can't publish it at /static/.
static/xlsx/
.env
*.egg-info/
.DS_Store
seed_data/*.csv
!seed_data/*.example.csv
